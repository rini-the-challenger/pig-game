//Selecting elements
const scoreEl = document.querySelectorAll('.score');
const diceEl = document.querySelector('.dice');
export const btnRollEl = document.querySelector('.btn--roll');
export const btnNewEl = document.querySelector('.btn--new');
export const btnHoldEl = document.querySelector('.btn--hold');
export const currScoreEl = document.querySelectorAll('.current-score');

export { scoreEl, diceEl };
